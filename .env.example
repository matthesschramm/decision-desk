# Optional. Without a key the app uses its deterministic guided synthesis.
OPENAI_API_KEY=

# Confirmed current model from the installed OpenAI SDK documentation.
OPENAI_MODEL=gpt-5.5

# Set to the public deployment URL for canonical social-preview links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
